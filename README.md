Create the .env shown as below

.env file

BASE_URI=https://petstore.swagger.io/v2
CONTENT_TYPE=application/json
TIMEOUT=30

Perform the below command to install the requirements

pip install -r requirements.txt

To run the tests, go to terminal and take the control to the root directory of the framework and run the below command

pytest -v
